fastapi==0.115.6
uvicorn[standard]==0.34.0
numpy==2.2.1
scipy==1.15.1
filterpy==1.4.5
pydantic==2.10.4
pytest==8.3.4
httpx==0.28.1
# Optional — only needed to regenerate ground-truth missions. The service and
# tests run without it (numpy mission generator is the fallback).
# jsbsim==1.2.1
